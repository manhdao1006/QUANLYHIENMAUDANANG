@model IEnumerable<QUANLYHIENMAUDANANG.Models.DONVITOCHUC>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="title_management">DANH SÁCH ĐƠN VỊ TỔ CHỨC</div>

<form method="get" action="@Url.Action("Index")">
    <input class="txt_search" name="keyword" placeholder="Nhập từ khóa tìm kiếm ..." value="@ViewBag.CurrentFilter" />
    <input class="btn_primary" type="submit" value="Tìm kiếm" />
</form>

<div class="link_add">
    <a href="~/DonViToChuc/Create">Thêm mới đơn vị tổ chức</a>
</div>

<table class="table" style="border: solid 2px black;">
    <tr>
        <th>
            Tên đơn vị tổ chức
        </th>
        <th>
           Địa chỉ
        </th>
        <th>
            Số điện thoại
        </th>
        <th>
            Người đại diện
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDVTC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiaChi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoDienThoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NguoiDaiDien)
            </td>
            <td>
                @Html.ActionLink("Cập nhật", "Edit", new { id = item.MaDVTC }) |
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaDVTC }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.MaDVTC })
            </td>
        </tr>
    }
</table>
